LAST COUNTY — Stage 28B validation

JS syntax: OK (all js/*.js and tools/*.js via node --check)

Asset generation:
- node tools/gen_props.js -> 231 props (was 196; 35 new underground props)

node tools/validate_assets.js:  OK
node tools/validate_stage27.js: OK (9 weapons, 9 arsenal items, 11 recipes)
node tools/validate_stage28.js: OK (8 resource/tool items, 8 mineral nodes, 14 recipes)

Ore distribution, 4 seeds, before -> after chamber carving:
  coal                8022 -> 7484
  iron                4429 -> 4117
  copper              5439 -> 4970
  quartz              2395 -> 2273
  nickel              2006 -> 1893
  rich iron           1283 -> 1270
  deep crystal         370 ->  360
  blackridge mineral   253 ->  253
  (~6% less rock volume; distribution and depth banding untouched)

node tools/validate_world.js 5: OK on all 5 seeds
- no props inside solid blocks
- no containers inside walls
- no unreachable containers
- interactive points and main routes intact
- per seed: ~2460-2551 props (was ~1197-1242)
            ~266-273 lights (was ~204-210)
            ~205-260 emitters (was ~114-129)

node tools/smoke.js: PASS

Chambers, 4 seeds (136 total):
  crystal 43 | flooded 24 | collapse 21 | camp 20 | blackridge 9 | dry 3
  skipped 16 (paved over by a metro/mall/mine gallery built after world gen)
  9 sealed pockets per seed, reachable only by mining

Regressions found and fixed during the pass:
- chambers carved too close to the surface undermined the ground the player
  stands on. The smoke test caught it as three melee failures (repair_hammer,
  sledgehammer, and heavy-vs-light all reporting zero damage) because the test
  target and the player were falling. Fixed by requiring 14 tiles of roof
  across the chamber's whole width, not just at its centre.
- the first dressing pass put 200 props inside solid rock and 2 containers
  inside walls: placement trusted one sampled floor row for a whole room.
  Fixed by resolving the floor and ceiling per column.
- the rock pass consumed the cells the themed props needed, so camps and
  sealed doors silently vanished. Fixed by reserving the room's centre first.
- a flooded chamber landed inside the underground mall. Fixed by skipping
  chambers a structure has since paved over.

Rendered for review (headless canvas, real game frames):
- crystal chamber, miner camp, collapsed gallery, flooded chamber,
  Blackridge sealed compartment

Deliberately NOT added:
- no new containers in natural caves. validate_world requires every container
  to be walkable-reachable from spawn, and natural caves are not (the validator
  already exempts cave_mouth points for the same reason). The rooms' reward is
  the crystal field — a real Stage 28 minable resource, seeded into the new
  rock faces — and the scene itself.
